{
  "name": "env-file-env-expansion",
  "version": "10.1.0",
  "description": "env-cmd env expansion example",
  "scripts": {
    "test": "env-cmd --verbose -x node index.js --path=\\$ENV_PATH"
  }
}
