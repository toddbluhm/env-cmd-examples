{
  "name": "rc-file-silent",
  "version": "10.1.0",
  "description": "env-cmd rc file silent flag example",
  "type": "module",
  "scripts": {
    "test": "cross-env ENV_PATH=./non-existent env-cmd --verbose --silent -e test -f ./non-existent -- node index.js"
  }
}
