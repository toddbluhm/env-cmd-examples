{
  "name": "rc-file-silent",
  "version": "10.1.0",
  "description": "env-cmd rc file silent flag example",
  "scripts": {
    "test": "cross-env ENV_PATH=./non-existent env-cmd --verbose --silent -e test -r ./non-existent node index.js"
  }
}
