{
  "name": "env-cmd-test",
  "version": "2.0.0",
  "description": "Test Project for env-cmd",
  "author": "Todd Bluhm",
  "license": "MIT",
  "scripts": {
    "lint": "standard --fix",
    "test": "cd ./fallback-example && npm test && cd ../file-example && npm test && cd ../rc-example && npm test && cd .."
  },
  "devDependencies": {
    "env-cmd": "^8.0.0",
    "standard": "^12.0.1"
  }
}
