{
  "name": "env-cmd-test",
  "version": "9.0.0",
  "description": "Example/Test Project for env-cmd",
  "author": "Todd Bluhm",
  "license": "MIT",
  "scripts": {
    "lint": "standard --fix",
    "test": "cd ./fallback-example && npm test && cd ../file-example && npm test && cd ../rc-example && npm test && cd ..",
    "postinstall": "cd ./fallback-example && npm i && cd ../file-example && npm i && cd ../rc-example && npm i && cd .."
  },
  "devDependencies": {
    "standard": "^14.0.0"
  }
}
